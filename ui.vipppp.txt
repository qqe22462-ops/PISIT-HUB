-- ====================================================================
-- PISIT x TATA | Main Hub UI (Fixed & Clean Link System)
-- ====================================================================

local successLib, Library = pcall(function()
    return loadstring(game:HttpGet("https://raw.githubusercontent.com/qqe22462-ops/PISIT-X-TATA/refs/heads/main/PISIT_HUB.bundle.lua"))()
end)

if not successLib or not Library then
    warn("[PISIT HUB] โหลด UI หลักไม่สำเร็จ!")
    return
end

local HttpService = game:GetService("HttpService")
local Players = game:GetService("Players")
local MarketplaceService = game:GetService("MarketplaceService")
local localPlayer = Players.LocalPlayer

local success, placeInfo = pcall(function()
    return MarketplaceService:GetProductInfo(game.PlaceId)
end)
local mapName = success and placeInfo.Name or "Unknown Map"

local totalUniqueUsers = 0

-- ระบบนับคนรันผ่าน Cloudflare
task.spawn(function()
    pcall(function()
        local workerUrl = "https://pisit-vip.qqe22462.workers.dev/"
        local data = { username = localPlayer.Name, userId = localPlayer.UserId, mapName = mapName, placeId = game.PlaceId }
        local encodedData = HttpService:JSONEncode(data)
        local httpRequest = request or http_request or syn and syn.request
        
        if httpRequest then
            local response = httpRequest({
                Url = workerUrl,
                Method = "POST",
                Headers = { ["Content-Type"] = "application/json", ["X-Pisit-Source"] = "PisitSecureHub2026" },
                Body = encodedData
            })
            if response and response.StatusCode == 200 then
                local decodedRes = HttpService:JSONDecode(response.Body)
                if decodedRes and decodedRes.totalUniqueUsers then
                    totalUniqueUsers = decodedRes.totalUniqueUsers
                end
            end
        end
    end)
end)

-- ไฟล์เสียง pisit.mp3
task.spawn(function()
    pcall(function()
        local soundFileName = "pisit.mp3"
        local soundRawLink = "https://raw.githubusercontent.com/qqe22462-ops/PISIT-X-TATA/refs/heads/main/pisit.mp3"
        if writefile and readfile and isfile and not isfile(soundFileName) then
            writefile(soundFileName, game:HttpGet(soundRawLink))
        end
        local sound = Instance.new("Sound")
        if getcustomasset and isfile and isfile(soundFileName) then
            sound.SoundId = getcustomasset(soundFileName)
        else
            sound.SoundId = soundRawLink
        end
        sound.Volume = 2
        sound.Parent = workspace
        sound:Play()
        task.delay(10, function() if sound then sound:Destroy() end end)
    end)
end)

-- สร้าง Window และ Tabs
local Window = Library:CreateWindow({ Title = "PISIT x TATA | Main Hub" })

local PlayerTab = Window:CreateTab({ Title = "Player" })
local CheatTab = Window:CreateTab({ Title = "ช่วยเล่น" })
local MapTab = Window:CreateTab({ Title = "สคริปต์ประจำแมพ" })
local SettingTab = Window:CreateTab({ Title = "Setting" })

-- Sections
local EspSection = PlayerTab:CreateSection({ Title = "Visuals (ESP)" })
local OptSection = PlayerTab:CreateSection({ Title = "Optimization & Performance" })
local CheatSection = CheatTab:CreateSection({ Title = "Script Executor Helper" })
local MapScriptSection = MapTab:CreateSection({ Title = "ระบบรันสคริปต์ประจำแมพ" })

local ProfileSection = SettingTab:CreateSection({ Title = "ข้อมูลผู้ใช้งานและแมพ" })
local StatsSection = SettingTab:CreateSection({ Title = "สถิติการใช้งานสคริปต์" })

-- SETTING TAB
ProfileSection:CreateButton({
    Title = "👤 ชื่อผู้รัน " .. localPlayer.Name .. " (ID: " .. localPlayer.UserId .. ")",
    Callback = function()
        game:GetService("StarterGui"):SetCore("SendNotification", { Title = "PISIT HUB", Text = localPlayer.Name, Duration = 2 })
    end
})

ProfileSection:CreateButton({
    Title = "🗺️ แมพปัจจุบัน " .. mapName,
    Callback = function()
        game:GetService("StarterGui"):SetCore("SendNotification", { Title = "PISIT HUB", Text = "PlaceId: " .. game.PlaceId, Duration = 2 })
    end
})

StatsSection:CreateButton({
    Title = "📊 จำนวนคนรันทั้งหมด " .. totalUniqueUsers .. " คน",
    Callback = function()
        game:GetService("StarterGui"):SetCore("SendNotification", { Title = "PISIT HUB", Text = "ใช้งานไปแล้ว " .. totalUniqueUsers .. " คน", Duration = 2 })
    end
})

-- PLAYER TAB
EspSection:CreateToggle({
    Title = "ESP ค่าย PISIT หลัก",
    Default = false,
    Callback = function(state)
        if state then
            pcall(function() if getgenv and getgenv().PisitEspCleanup then getgenv().PisitEspCleanup() end end)
            task.spawn(function() loadstring(game:HttpGet("https://raw.githubusercontent.com/qqe22462-ops/PISIT-X-TATA/refs/heads/main/ESP.PISIT.HUB"))() end)
        else
            pcall(function() if getgenv and getgenv().PisitEspCleanup then getgenv().PisitEspCleanup() end end)
        end
    end
})

OptSection:CreateButton({
    Title = "ลดความแลค ค่าย PISIT",
    Callback = function()
        task.spawn(function() loadstring(game:HttpGet("https://raw.githubusercontent.com/qqe22462-ops/PISIT-X-TATA/refs/heads/main/FPS.PISIT.HUB"))() end)
    end
})

-- CHEAT TAB
CheatSection:CreateButton({
    Title = "ESP อิสระค่าย PISIT",
    Callback = function()
        task.spawn(function() loadstring(game:HttpGet("https://raw.githubusercontent.com/qqe22462-ops/PISIT-X-TATA/refs/heads/main/ESP.AII.PISIT"))() end)
    end
})

CheatSection:CreateButton({
    Title = "สคริปผลักคน",
    Callback = function()
        task.spawn(function() loadstring(game:HttpGet("https://raw.githubusercontent.com/qqe22462-ops/PISIT-X-TATA/refs/heads/main/PISIT%20X%20TATA"))() end)
    end
})

CheatSection:CreateButton({
    Title = "สคริปบิน",
    Callback = function()
        task.spawn(function() loadstring(game:HttpGet("https://raw.githubusercontent.com/qqe22462-ops/PISIT-X-TATA/refs/heads/main/fly.lua"))() end)
    end
})

CheatSection:CreateButton({
    Title = "สคริปต์หยุดเวลา",
    Callback = function()
        task.spawn(function() loadstring(game:HttpGet("https://raw.githubusercontent.com/qqe22462-ops/PISIT-X-TATA/refs/heads/main/Stop"))() end)
    end
})

CheatSection:CreateButton({
    Title = "สคริป สุคุนะ x PISIT",
    Callback = function()
        task.spawn(function() loadstring(game:HttpGet("https://raw.githubusercontent.com/qqe22462-ops/PISIT-X-TATA/refs/heads/main/Sukuna.x.PISIT"))() end)
    end
})

CheatSection:CreateButton({
    Title = "สคริป โกโจ x PISIT",
    Callback = function()
        task.spawn(function() loadstring(game:HttpGet("https://raw.githubusercontent.com/qqe22462-ops/PISIT-X-TATA/refs/heads/main/GOJO"))() end)
    end
})

CheatSection:CreateButton({
    Title = "สคริปต์ นมใหญ่",
    Callback = function()
        task.spawn(function() loadstring(game:HttpGet("https://raw.githubusercontent.com/clemonlang/clemon_roclothes/refs/heads/main/ClemonRC.lua"))() end)
    end
})

-- ====================================================================
-- MAP TAB: ปุ่มกดตรง รันลิงก์ตรวจแมพออโต้ของมึงทันที
-- ====================================================================
MapScriptSection:CreateButton({
    Title = "🟢 รันสคริปต์ประจำแมพ",
    Callback = function()
        task.spawn(function()
            loadstring(game:HttpGet("https://raw.githubusercontent.com/qqe22462-ops/PISIT-HUB/refs/heads/main/mapAII.aoto.PISIT_HUB"))()
        end)
        
        game:GetService("StarterGui"):SetCore("SendNotification", {
            Title = "PISIT HUB",
            Text = "กำลังโหลดระบบตรวจสอบและรันสคริปต์ประจำแมพ...",
            Duration = 2
        })
    end
})
